flask>=3.0
waitress>=3.0

# Phase 8 (2026-08-31): the Schedules page needs a real IANA time zone list
# and common/schedule_eval.py's zoneinfo-based evaluation -- see
# controller/requirements.txt's own comment on this same package for why.
tzdata>=2024.1

# 2026-09-07 (RoadMap.md's dated entry): real AdGuard/dashboard credential
# unification, replacing the earlier "just show the plaintext password on
# screen" approach the project owner correctly flagged as insecure.
# dashboard/adguard_config_sync.py writes AdGuard Home's OWN
# AdGuardHome.yaml directly (its REST API has no password-change endpoint
# at all -- confirmed live against a real v0.107.79 instance, see that
# module's own docstring) -- PyYAML for a safe structured read-modify-write
# (not regex-editing a real YAML file), bcrypt to generate a hash AdGuard's
# own Go bcrypt validator accepts (confirmed live: a hash generated by
# THIS library, injected into the real production AdGuardHome.yaml and
# restarted, authenticated successfully; cleaned up immediately after).
pyyaml>=6.0
bcrypt>=4.0
